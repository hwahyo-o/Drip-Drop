let map;let cafeLayer;let userMarker;
export function initMap({onCafeSelect}){map=L.map("map",{zoomControl:true}).setView([37.5665,126.978],12);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>'}).addTo(map);cafeLayer=L.layerGroup().addTo(map);map.on("click",()=>onCafeSelect(null));return map;}
export function renderCafeMarkers(cafes,onCafeSelect){if(!cafeLayer)return;cafeLayer.clearLayers();cafes.forEach((cafe)=>{if(!cafe.lat||!cafe.lng)return;L.marker([Number(cafe.lat),Number(cafe.lng)]).bindPopup(`<strong>${escapeHtml(cafe.name)}</strong><br>${escapeHtml(cafe.address)}`).on("click",()=>onCafeSelect(cafe)).addTo(cafeLayer);});}
export function fitToCafes(cafes){const points=cafes.filter((cafe)=>cafe.lat&&cafe.lng).map((cafe)=>[Number(cafe.lat),Number(cafe.lng)]);if(!map||!points.length)return;map.fitBounds(points,{padding:[40,40],maxZoom:15});}
export function locateUser(){return new Promise((resolve,reject)=>{if(!navigator.geolocation){reject(new Error("현재 브라우저에서 위치 기능을 지원하지 않습니다."));return;}navigator.geolocation.getCurrentPosition((position)=>{const location={lat:position.coords.latitude,lng:position.coords.longitude,accuracy:position.coords.accuracy};showUserLocation(location);resolve(location);},(error)=>reject(error),{enableHighAccuracy:true,timeout:10000,maximumAge:60000});});}
export function showUserLocation(location){if(!map)return;if(userMarker)userMarker.remove();const icon=L.divIcon({className:"user-location",iconSize:[18,18]});userMarker=L.marker([location.lat,location.lng],{icon}).addTo(map).bindPopup("현재 위치");map.setView([location.lat,location.lng],14);}
export function distanceKm(lat1,lng1,lat2,lng2){const radius=6371;const dLat=toRad(lat2-lat1);const dLng=toRad(lng2-lng1);const a=Math.sin(dLat/2)**2+Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;return radius*2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));}
function toRad(value){return value*Math.PI/180;}function escapeHtml(value){return String(value||"").replace(/[&<>"']/g,(char)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[char]);}
