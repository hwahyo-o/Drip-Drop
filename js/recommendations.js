import { distanceKm } from "./map.js";
export function recommendCafes(cafes,profile,userLocation){const tastes=tokenize(profile?.taste||"");const wanted=tokenize(profile?.wantedBeans||"");const owned=tokenize(profile?.ownedBeans||"");return cafes.map((cafe)=>{let score=0;const cafeText=tokenize([cafe.name,cafe.description,...(cafe.tags||[]),...(cafe.beans||[]).map((bean)=>`${bean.name} ${bean.notes}`)].join(" "));score+=overlap(tastes,cafeText)*8;score+=overlap(wanted,cafeText)*10;score+=overlap(owned,cafeText)*4;if(cafe.beanSales)score+=8;if((cafe.brewMethods||[]).length)score+=6;if(cafe.rating>=4.5)score+=5;if(userLocation&&cafe.lat&&cafe.lng){const km=distanceKm(userLocation.lat,userLocation.lng,Number(cafe.lat),Number(cafe.lng));score+=Math.max(0,12-km);}return {cafe,score};}).sort((a,b)=>b.score-a.score).slice(0,5);}
function tokenize(value){return String(value||"").toLowerCase().split(/[\s,/#]+/).map((item)=>item.trim()).filter(Boolean);}
function overlap(left,right){const rightSet=new Set(right);return left.filter((item)=>rightSet.has(item)).length;}
